package com.example.course_schedule_for_chd_v002.ui.screens.schedule

import org.junit.Ignore

/**
 * ScheduleViewModel 单元测试
 *
 * ⚠ @Ignore：本测试基于 v37/v61 之前的旧 API（`refreshSchedule` / `isRefreshing` /
 * 构造缺 `userPreferences`），与当前 `ScheduleViewModel` 不匹配，整体忽略。
 * 待基于当前 API 重写（见 [docs/OPEN-DECISIONS.md](../../../../../docs/OPEN-DECISIONS.md)）。
 * 保留此文件占位，仅为不丢失"此处应有测试"的信号。
 */
@Ignore("待基于当前 ScheduleViewModel API 重写（OPEN-DECISIONS）")
class ScheduleViewModelTest
