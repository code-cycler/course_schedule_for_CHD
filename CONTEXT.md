# 上下文术语表（CONTEXT.md）

> 本文件只存「这个词在本项目里是什么意思」—— 不包含实现细节。
> 技术决策、未决问题分别见 `docs/adr/` 与 `docs/OPEN-DECISIONS.md`。

## 签到辅助

在本项目中，「签到辅助」指：当用户收到畅课（学习通）的签到通知时，本 App 识别该通知并提醒用户，同时把系统虚拟定位切换到用户预先标定的签到位置；最终的签到动作仍由用户在学习通 App 内手动完成。

**不包含**：自动点击签到、破解/调用学习通签到接口、绕过验证码/手势/二维码等校验。

## 畅课

「畅课」是本功能监听的目标应用，指 TronClass（西安智园软件出品，Android 包名 `com.wisdomgarden.trpc`）。其发出的系统通知是触发签到辅助的信号源。

## 签到位置

「签到位置」是用户为签到场景保存的地理坐标（纬度/经度/精度半径/备注等），以独立记录形式存在；它**通过采集设备当前的 GPS 定位获得**（坐标天然为 WGS-84）。位置 Optionally 可以关联到课程表中的某门课程或某个教室，用于在签到触发时优先自动匹配目标位置；未关联时，由用户手动选择使用哪个位置。

## 虚拟定位

「虚拟定位」指本 App 在收到畅课签到通知后，通过 Android 的测试定位提供者（Test Provider）把系统定位临时改写为用户预先标定的签到位置。在本项目中，虚拟定位是一次性的：设置后保持一段用户可配置的短时长（范围 1–10 分钟，默认 10 分钟），由用户手动关闭或超时自动恢复真实定位；最终签到动作仍由用户在学习通 App 内完成。

「自动打开畅课」是一个可由用户在设置中开关的选项，默认关闭；开启后，触发签到辅助时本 App 会自动启动畅课 App。

## 签到通知识别

「签到通知识别」指本 App 的 NotificationListenerService 通过匹配通知来源包名（`com.wisdomgarden.trpc`）以及标题/正文中的关键字（如「签到」「考勤」「点名」）来判断当前收到的系统通知是否属于签到通知。识别成功后触发虚拟定位与提醒。

## 触发时选位置

「触发时选位置」指收到签到通知后，系统决定使用哪个已保存签到位置的规则。在本项目中，规则为：若只保存了一个位置，则直接使用；否则优先按当前课程/教室自动匹配，并参考用户对该课程/教室上次手动选择的位置记忆；若仍无法确定，则交由用户手动选择。
